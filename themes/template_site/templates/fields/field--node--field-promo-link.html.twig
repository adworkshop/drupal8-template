{% for item in items %}
    <div class="button">
        <a class="button__link" href="{{ item.content['#url'].toString() }}">
            {{ item.content['#title'] }}
        </a>
    </div>
{% endfor %}