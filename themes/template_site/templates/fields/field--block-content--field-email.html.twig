{% for item in items %}
<div class = "footer__contact_info">
		<div class = "footer__label">Email:</div>
		<div class = "footer__client_info">
			<a href="{{ remove_comments(item.content|render) }}">{{ item.content }}</a>
		</div>
</div>
{% endfor %}