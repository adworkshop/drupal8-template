{% for item in items %}
    <div class="button  wow fadeIn animated">
    	<a href="{{ item.content['#url'] }}" class="button__link">{{ item.content['#title'] }}</a>
    </div><!--END .button-->
{% endfor %}